<form method="post" action="{{ path('app_pato_delete', {'id': pato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pato.id) }}">
    <button class="btn btn-danger minimarg">Delete</button>
</form>
